{
  "name": "onrecord",
  "private": true,
  "version": "0.1.0",
  "description": "On Record — an on-chain-first radar for novel Solana programs. Every deploy, deduped and ranked by novelty.",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "dev:ingest": "pnpm --filter @onrecord/ingest dev",
    "dev:worker": "pnpm --filter @onrecord/ingest dev:worker",
    "dev:web": "pnpm --filter @onrecord/web dev",
    "snapshot": "node scripts/snapshot.mjs",
    "dev:real": "node scripts/snapshot-api.mjs",
    "dev:mock": "node scripts/mock-api.mjs",
    "dev:design": "node scripts/snapshot-api.mjs & API_URL=http://localhost:3001 pnpm --filter @onrecord/web dev",
    "db:push": "pnpm --filter @onrecord/core db:push",
    "db:generate": "pnpm --filter @onrecord/core db:generate",
    "db:migrate": "pnpm --filter @onrecord/core db:migrate",
    "seed": "pnpm --filter @onrecord/enrich seed"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.0.0"
}
